.comment-form
  = form_for :comment, url: "/reviews/#{@review.id}/comments", method: 'POST' do |f|
    -if @errors
      - @errors.each do |error|
        = error
    = f.text_area :text
    = f.hidden_field :user_id, value: current_user.id

    =f.submit
