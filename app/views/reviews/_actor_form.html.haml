.review-form
  = form_for :review, url: "/actors/#{@actor.id}/reviews", method: 'POST' do |f|
    -if @errors
      - @errors.each do |error|
        = error
    = f.text_area :text
    = f.hidden_field :user_id, value: current_user.id

    =f.submit
